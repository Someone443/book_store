.hidden-xs.clearfix
  .dropdowns.pull-right
    .dropdown.width-240
      %a.dropdown-toggle.lead.small{href: "#", 'data-toggle': "dropdown", role: "button", 'aria-haspopup': "true", 'aria-expanded': "false"}
        = t(@book_filter.current_sort)
        %i.fa.fa-angle-down.dropdown-icon.pull-right
      %ul.dropdown-menu
        %li
          = link_to t(:newest), request.params.slice(:page, :sort).except(:sort)
        %li
          = link_to t(:popular), request.params.slice(:page, :sort).merge(sort: 'popular')
        %li
          = link_to t(:price), request.params.slice(:page, :sort).merge(sort: 'price')
        %li
          = link_to t(:price_desc), request.params.slice(:page, :sort).merge(sort: 'price_desc')
  %ul.list-inline.pt-10.mb-25.mr-240
    %li.mr-35 
      %a.filter-link{href: books_path, class: ("active" unless @category)}
        = t(:category_all)
        %span.badge.general-badge= Book.count
    - @categories.each do |category|
      %li.mr-35
        %a.filter-link{href: books_category_path(category), class: ("active" if @category == category)}
          = category.name
          %span.badge.general-badge= category.books.count
